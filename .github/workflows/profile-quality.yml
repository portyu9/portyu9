name: Profile quality

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - "README.md"
      - "SVG_OPTIONS.md"
      - "assets/**"
      - "concepts/profile-svg/**"
      - "scripts/**"
      - ".github/workflows/profile-quality.yml"

permissions:
  contents: read

concurrency:
  group: profile-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate live profile cleanup contract
        run: python3 scripts/validate-profile.py

      - name: Validate SVG concept proportions and vector contract
        run: python3 scripts/validate-svg-concepts.py
